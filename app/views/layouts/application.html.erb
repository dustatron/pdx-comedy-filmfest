<!DOCTYPE html>
<html>
  <head>
    <title>Portland's Funniest Video</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.7.1/css/all.css" integrity="sha384-fnmOCqbTlWIlj8LyTjo7mOUStjsKC4pOpQbqyi7RrhN7udi9RwhKkMHpvLbHG9Sr" crossorigin="anonymous">
  </head>

  <body>
    <!-- Fixed navbar -->
    <nav class="navbar navbar-inverse navbar-fixed-top">
      <div class="container">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <%= link_to "Portland's Funniest Video", static_pages_home_path, class: "navbar-brand" %>
            <!--<a href="https://curiouscomedy.org/" target="_blank"><img class="logo" src="http://curiouscomedy.org/images/logo.png" alt="Curious Comedy Theater" ></a>-->
        </div>

        <div id="navbar" class="navbar-collapse collapse navbar-right">
          <ul class="nav navbar-nav">
            <li class="active"><%= link_to "Home", static_pages_home_path %></li>
            <li><%= link_to "About", static_pages_about_path %></li>
            <li><%= link_to "HowTo", static_pages_help_path %></li>            
            <li><%= link_to "Submit", new_submission_path %></li>

            <% if logged_in? %>
              <li class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                  Account <b class="caret"></b>
                </a>
                <ul class="dropdown-menu">
                  <li><%= link_to "Profile", current_user %></li>
                  <li><%= link_to "Settings", edit_user_path(current_user) %></li>
                  <li class="divider"></li>
                  <li>
                    <%= link_to "Log out", logout_path, method: :delete %>
                  </li>
                </ul>
              </li>
            <% else %>
              <li><%= link_to "Signup", signup_path %></li>
            <% end %>


            
        </div><!--/.nav-collapse -->
      </div>
    </nav>
    <body>
      <% if logged_in? && current_user.admin? %>
        <%= render '/shared/admin_view'%>
      <% end %>
    <!--BODY-->
    <div class="container pad-top">
      <div class="col-md-12 ">
      
          <% flash.each do |message_type, message| %>
            <%= content_tag(:div, message, class: "home--top alert alert-#{message_type}") %>
          <% end %>
          
          <%= yield %>
          
          </br>
          <%= debug(params) if Rails.env.development? %>
       
      </div>
    </div>
  </body>
</html>
