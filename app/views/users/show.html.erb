<div class="row">
  <div class="col-md-6">
    <%= gravatar_for @user %>
    <h3>
      <%= @user.name %>
    </h3>
    A Member for <%= time_ago_in_words(current_user.created_at) %></br>
    <%= mail_to @user.email %>
  </div>
  <div class="col-md-6">
  </div>

</div><!--END ROW-->
<div class="row">
  
    
    <% if @submissions.any? %>
      <h3 class='text-center'>Submissions (<%= @submissions.count %>)</h3>
      <ol class="microposts">
        <%= render @submissions %>
      </ol>
      <%= will_paginate @submissions %>
      <div class="row">
        <div class="col-md-8 col-md-offset-2">
          <%= link_to "Submit Video", new_submission_path, class: "btn btn-primary btn-lg btn-block" %>
        </div>
        
      </div>
    <% else %>
      <h3 class= "text-center">Submissions</h3>
      <div class="well text-center">
        <h4> You Have No Submissions Yet </h4>

      </div>  
    <% end %>
</div>
  
