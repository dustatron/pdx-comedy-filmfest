open files from terminal 
    c9 open 'file names'

Run server
    $ rails s -b $IP -p $PORT
    
Create Branch and go to it.    
    $ git checkout -b basic_login
    
    
    <% if @user.microposts.any? %>
      <h3>Microposts (<%= @user.microposts.count %>)</h3>
      <ol class="microposts">
        <%= render @microposts %>
      </ol>
      <%= will_paginate @microposts %>
    <% end %>